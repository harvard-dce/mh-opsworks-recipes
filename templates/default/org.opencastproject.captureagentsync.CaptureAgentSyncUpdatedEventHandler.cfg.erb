org.opencastproject.capture-agent-notifier=<%= @capture_agent_sync_url %>

# Notify a CA if a schedule has a start or end time within this many seconds from now
# This should be less than the shortest capture agent schedule fetch period 
org.opencastproject.capture-agent-notifier.threshold=<%= @capture_agent_sync_threshold %>
